import React from "react";
import TextEditor from "../components/TextEditor"; // Adjust path if needed

const TextEditorPage: React.FC = () => {
  return (
    <div className="page-container">
      <h1>Text Editor</h1>
      <TextEditor />
    </div>
  );
};

export default TextEditorPage;
